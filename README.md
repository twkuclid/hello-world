# hash key(#) + space  for title

- dash + space for black dot
  - tab + dash+ space in next line for white dot
    - tab + dash+ space in next line for rectangle
      - adding indent wont change the shape from now on.U only get rectangle.
  
